// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("rlog", "for rlog support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("rlog", "enable rlog support", "no");

if (PHP_RLOG != "no") {
	EXTENSION("rlog", "rlog.c");
}

